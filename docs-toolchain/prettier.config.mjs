// The Documentation capability must format Markdown exactly the way the Source capability
// does. Both resolve the same shared export rather than Prettier's own defaults, which is
// the whole point of this file existing instead of an inline `{}`.
export { default } from '@mikode13/code-style/prettier';
